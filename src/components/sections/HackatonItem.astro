---
import SparklesText from '@/islands/SparklesText'

interface Props {
  title: string
  place: string
  position?: string
  date: string
}

const { title, place, position, date } = Astro.props
---

<div
  class='relative mx-12 pb-4 grid before:absolute before:left-[-35px] before:block before:h-full before:border-l-2 before:border-black/20 dark:before:border-white/15 before:content-[''] md:grid-cols-5 md:gap-10 md:space-x-4]'
>
  <div class='relative pb-4 md:col-span-2'>
    <div class='sticky top-0'>
      <span class='text-yellow-400 dark:text-purple-600 -left-[42px] absolute rounded-full text-5xl'>
        &bull;
      </span>

      {position && (
        <SparklesText
          client:only
          text={position}
        />
      )}

      <h3 class='text-xl font-semibold text-yellow-400 dark:text-purple-500'>
        {title}
      </h3>

      <h4 class='p-0 m-0 text-sm font-semibold text-gray-600 dark:text-white'>
        {place}
      </h4>

      <time class='p-0 m-0 text-sm text-gray-600/80 dark:text-white/80'>
        {date}
      </time>
    </div>
  </div>
</div>
